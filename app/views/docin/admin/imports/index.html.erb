<%= form_with model: false, scope: :item, url: { action: :create }, html: { method: :post, multipart: true } do |f| %>
  <%= render 'form', f: f %>
  <div class="core-submitters">
    <%= f.submit :confirm %>
  </div>
<% end %>
